<p style="color: green"><%= notice %></p>

<h1>Coins</h1>

<div id="coins">
  <% @coins.each do |coin| %>
    <%= render coin %>
    <p>
      <%= link_to t('links.show'), coin %>
    </p>
  <% end %>
</div>

<%= link_to "New coin", new_coin_path %>
