# all things in ruby is a object
# so you can applicate a ".class" 

# in irb > type some thing and pess TAB
# you can see all methods of the object
# example: irb > 23 or "Hello" > . > PRESS TAB

/ IN RUBY: the primitive types already are complex types, because are generating like a objects /